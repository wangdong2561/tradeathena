# TradeAthena — Python 依赖
# 安装: pip install -r requirements.txt

# ── Web 框架 ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.38.0

# ── 数据校验 ───────────────────────────────────────────
pydantic>=2.9.0

# ── 数据库 ─────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.35
aiosqlite>=0.20.0
greenlet>=3.0.0

# ── Auth ─────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ── HTTP 客户端 / WebSocket ────────────────────────────
httpx>=0.27.0
websockets>=13.0

# ── 环境变量 ─────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Rust 构建工具 (首次安装需要) ────────────────────────
# maturin>=1.0.0
